name: Quality Gates

on:
  pull_request:
  push:
    branches:
      - develop
      - main
  merge_group:

jobs:
  quality:
    uses: ./.github/workflows/reusable-rust-quality.yml
    with:
      validate_docs: true
